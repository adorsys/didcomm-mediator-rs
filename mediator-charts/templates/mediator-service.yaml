apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mediator-service
spec:
  selector:
    app: {{ .Release.Name }}-mediator
  ports:
      - protocol: TCP
        port: 8080
        targetPort: 8080
  type: LoadBalancer