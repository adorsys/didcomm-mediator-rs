apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-mediator
  ports:
  - name: https
    port: 3000
    targetPort: 3000
  type: ClusterIP
